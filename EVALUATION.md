# 평가 보고서 — 피싱 분석 챗봇

- **대상**: 피싱 분석 챗봇 (Streamlit + OpenAI)
- **저장소**: github.com/goldenus38/chatbot
- **배포 URL**: https://chatbot-xodua96btqu3kzczrx9hpg.streamlit.app/
- **평가일**: 2026-05-28
- **맥락**: 바이브코딩 교육 과정 실습 과제

---

## 1. 종합 요약

피싱(이메일·문자·URL) 분석이라는 **명확한 보안 주제**를 가진 챗봇으로, 과제의 핵심 요구사항(제공사·모델 선택, 사전 API 키 등록)을 모두 충족한다. 코드는 4개 모듈로 깔끔하게 분리되어 있고, API 키를 서버 사이드에 두는 **보안적으로 올바른 설계**를 채택했다. 실서비스 수준의 견고함(비용 제어, 입력 검증, 테스트)은 부족하지만, **교육 실습 과제로서는 완성도가 높다.**

| 항목 | 점수 |
|---|:---:|
| 요구사항 충족도 | ★★★★★ (5/5) |
| 기능성 | ★★★★☆ (4/5) |
| UX / 디자인 | ★★★★☆ (4/5) |
| 코드 품질 | ★★★★☆ (4/5) |
| 보안 | ★★★★☆ (4/5) |
| **종합** | **★★★★☆ (4.2/5)** |

---

## 2. 요구사항 충족도

| 요구사항 | 충족 | 비고 |
|---|:---:|---|
| 챗봇 사용 전 제공사(ChatGPT/Claude) 선택 | ✅ | 사이드바 셀렉트박스 |
| 모델 선택 | ✅ | 제공사별 모델 목록 |
| 사전 설정에서 API 키 등록 | ✅ | 사이드바 입력 + Secrets 자동 로드 |
| 챗봇 형태 | ✅ | `st.chat_input` 기반 대화형 |
| 기술 스택 (React/Streamlit 중 택1) | ✅ | Streamlit |
| 사이버보안 주제 | ✅ | 피싱 분석 |

> ⚠️ **부분 충족**: 제공사 선택지에 Claude가 노출되지만 `enabled=False`로 **실제 동작은 ChatGPT만** 가능하다. "선택 UI 제공"이라는 요구는 만족하나, 채점 기준이 "두 제공사 모두 동작"이라면 미달이다. (의도된 범위 축소 — OpenAI 키만 사용하기로 결정)

---

## 3. 강점

1. **올바른 보안 설계**: API 키를 브라우저가 아닌 서버(Streamlit 세션/Secrets)에서 다룬다. React 단독 구현 시 발생하는 키 노출 문제를 구조적으로 회피 — 보안 주제 앱으로서 일관성 있음.
2. **명확한 모듈 분리**: `config.py`(설정·프롬프트), `chat_client.py`(API 호출), `styles.py`(디자인), `app.py`(UI/흐름)로 책임이 분리되어 가독성·유지보수성이 좋다.
3. **실용적 UX**: 샘플 버튼 3종으로 진입 장벽을 낮췄고, 키 미등록·Claude 선택 시 게이트(안내 메시지 + 입력 비활성)로 오류 상황을 사전 차단한다.
4. **스트리밍 응답**: `st.write_stream`으로 토큰 단위 출력 → 체감 반응성이 좋다.
5. **구조화된 분석 출력**: 시스템 프롬프트로 위험도→신호→근거→대응 형식을 고정해 결과 일관성이 높고, 면책 문구로 과신을 방지한다.
6. **친화적 오류 처리**: 인증·연결·스트리밍 중단 오류를 사용자 메시지로 변환(앱이 죽지 않음).

---

## 4. 약점 및 개선점

| 우선순위 | 항목 | 설명 | 개선안 |
|:---:|---|---|---|
| 🔴 높음 | **비용 남용 위험** | 공개 URL + Secrets 내장 키 조합은 누구나 접속해 소유자 할당량을 소모할 수 있다. | OpenAI 대시보드에서 **월 사용 한도 설정**, 또는 간단한 접근 비밀번호/요청 수 제한 추가 |
| 🟡 중간 | **토큰/비용 제어 없음** | `max_tokens` 미설정, 입력 길이 검증 없음 → 긴 입력 시 비용·오류 가능 | `max_tokens` 지정, 입력 글자 수 상한 |
| 🟡 중간 | **모델 목록 하드코딩** | `config.py`에 모델명 고정 → 신규/폐기 모델 미반영 | 동적 조회 또는 주기적 갱신 |
| 🟢 낮음 | **대화 기록 비영속** | 새로고침 시 대화 소실(세션 한정) | 필요 시 로컬/DB 저장 (과제 범위 밖) |
| 🟢 낮음 | **테스트 부재** | 자동화 테스트 없음 | `chat_client` 모킹 단위 테스트 |
| 🟢 낮음 | **분석 정확도 한계** | LLM 판단이라 오탐/미탐 가능 | 면책 문구로 완화 중(적절). 규칙 기반 보조 지표 병행 가능 |

---

## 5. 보안 평가

- ✅ 키 입력 `type="password"`, 서버 사이드 보관, `.streamlit/secrets.toml`은 `.gitignore`로 저장소 노출 차단.
- ✅ 분석 목적이 **방어적**(피싱 식별·예방)이며 시스템 프롬프트가 공격용 콘텐츠 생성을 거부하도록 지시.
- ⚠️ **가장 큰 리스크는 공개 배포본의 비용 노출** (4번 표 참조). 교육용 임시 키라 영향은 제한적이나, 운영 시에는 반드시 한도/접근 제어 필요.

---

## 6. 검증 범위 (정직성 고지)

본 평가는 다음에 근거한다:
- ✅ **코드 정적 분석** (전체 소스 확인)
- ✅ **API 엔드투엔드 동작** (실제 키로 피싱 분석 응답 정상 확인)
- ✅ **로컬 앱 기동** (오류 없음), **배포 응답** (HTTP 200)

다음은 본 평가에서 **육안 확인하지 못함** (브라우저 미사용):
- ⬜ 배포본의 디자인 렌더링 (다크 사이드바 가독성 등)
- ⬜ 배포본 라이브 채팅 동작 및 Secrets 등록 여부

> 위 ⬜ 항목은 배포 URL을 직접 열어 확인 권장.

---

## 7. 결론

교육 실습 과제의 목표(동작하는 보안 테마 챗봇 + 제공사/모델 선택 + 키 설정)를 **모두 달성**했으며, 코드 구조와 보안 설계가 모범적이다. 실서비스로 확장하려면 **비용 제어와 접근 제한**이 최우선 과제다. 현 상태로 **과제 제출에 충분한 완성도**로 판단된다.
